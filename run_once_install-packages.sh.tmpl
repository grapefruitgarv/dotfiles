#!/bin/bash

# This script runs ONLY ONCE when you first set up a new machine.
echo "New machine detected: Installing packages from ~/.private/Brewfile..."

if command -v brew &> /dev/null; then
    brew bundle --file "$HOME/.private/Brewfile"
else
    echo "Homebrew not found. Please install Homebrew first."
fi